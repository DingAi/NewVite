/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The RoleLevelRelForm model module.
 * @module com.mn.sdk.model/RoleLevelRelForm
 * @version 1.0
 */
export class RoleLevelRelForm {
  /**
   * Constructs a new <code>RoleLevelRelForm</code>.
   * 角色类别表单信息
   * @alias module:com.mn.sdk.model/RoleLevelRelForm
   * @class
   * @param check {Array.<Number>} 控制类别ids(前端传值用于修改)
   * @param control {Array.<Number>} 角色有查看变量的类别(前端传值用于修改)
   * @param roleId {String} 角色id
   */
  constructor(check, control, roleId) {
    this.check = check;
    this.control = control;
    this.roleId = roleId;
  }


    static constructFromList(list) {
        let array = [new RoleLevelRelForm()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(RoleLevelRelForm.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>RoleLevelRelForm</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/RoleLevelRelForm} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/RoleLevelRelForm} The populated <code>RoleLevelRelForm</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new RoleLevelRelForm();
      if (Reflect.has(data, 'check'))
        obj.check = ApiClient.convertToType(data['check'], ['Number']);
      if (Reflect.has(data, 'control'))
        obj.control = ApiClient.convertToType(data['control'], ['Number']);
      if (Reflect.has(data, 'roleId'))
        obj.roleId = ApiClient.convertToType(data['roleId'], 'String');
    }
    return obj;
  }
}

/**
 * 控制类别ids(前端传值用于修改)
 * @member {Array.<Number>} check
 */
RoleLevelRelForm.prototype.check = undefined;

/**
 * 角色有查看变量的类别(前端传值用于修改)
 * @member {Array.<Number>} control
 */
RoleLevelRelForm.prototype.control = undefined;

/**
 * 角色id
 * @member {String} roleId
 */
RoleLevelRelForm.prototype.roleId = undefined;


