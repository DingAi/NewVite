/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';
import {PlcStatusVo} from './PlcStatusVo';

/**
 * The DeviceStatusInfoVo model module.
 * @module com.mn.sdk.model/DeviceStatusInfoVo
 * @version 1.0
 */
export class DeviceStatusInfoVo {
  /**
   * Constructs a new <code>DeviceStatusInfoVo</code>.
   * @alias module:com.mn.sdk.model/DeviceStatusInfoVo
   * @class
   * @param id {String} 设备id
   * @param name {String} 设备名称
   * @param offLineCount {Number} 离线次数
   * @param offLineDuration {Number} 离线时长(秒)
   * @param onLineDuration {Number} 在线时长(秒)
   * @param onLineTime {String} 在线时间(天/时/分/秒)
   * @param onlineStatus {Boolean} 设备的在线状态,true:在线，false 离线
   * @param plcList {Array.<module:com.mn.sdk.model/PlcStatusVo>} plc的状态信息
   */
  constructor(id, name, offLineCount, offLineDuration, onLineDuration, onLineTime, onlineStatus, plcList) {
    this.id = id;
    this.name = name;
    this.offLineCount = offLineCount;
    this.offLineDuration = offLineDuration;
    this.onLineDuration = onLineDuration;
    this.onLineTime = onLineTime;
    this.onlineStatus = onlineStatus;
    this.plcList = plcList;
  }


    static constructFromList(list) {
        let array = [new DeviceStatusInfoVo()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(DeviceStatusInfoVo.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>DeviceStatusInfoVo</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/DeviceStatusInfoVo} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/DeviceStatusInfoVo} The populated <code>DeviceStatusInfoVo</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new DeviceStatusInfoVo();
      if (Reflect.has(data, 'id'))
        obj.id = ApiClient.convertToType(data['id'], 'String');
      if (Reflect.has(data, 'name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (Reflect.has(data, 'offLineCount'))
        obj.offLineCount = ApiClient.convertToType(data['offLineCount'], 'Number');
      if (Reflect.has(data, 'offLineDuration'))
        obj.offLineDuration = ApiClient.convertToType(data['offLineDuration'], 'Number');
      if (Reflect.has(data, 'onLineDuration'))
        obj.onLineDuration = ApiClient.convertToType(data['onLineDuration'], 'Number');
      if (Reflect.has(data, 'onLineTime'))
        obj.onLineTime = ApiClient.convertToType(data['onLineTime'], 'String');
      if (Reflect.has(data, 'onlineStatus'))
        obj.onlineStatus = ApiClient.convertToType(data['onlineStatus'], 'Boolean');
      if (Reflect.has(data, 'plcList'))
        obj.plcList = ApiClient.convertToType(data['plcList'], [PlcStatusVo]);
    }
    return obj;
  }
}

/**
 * 设备id
 * @member {String} id
 */
DeviceStatusInfoVo.prototype.id = undefined;

/**
 * 设备名称
 * @member {String} name
 */
DeviceStatusInfoVo.prototype.name = undefined;

/**
 * 离线次数
 * @member {Number} offLineCount
 */
DeviceStatusInfoVo.prototype.offLineCount = undefined;

/**
 * 离线时长(秒)
 * @member {Number} offLineDuration
 */
DeviceStatusInfoVo.prototype.offLineDuration = undefined;

/**
 * 在线时长(秒)
 * @member {Number} onLineDuration
 */
DeviceStatusInfoVo.prototype.onLineDuration = undefined;

/**
 * 在线时间(天/时/分/秒)
 * @member {String} onLineTime
 */
DeviceStatusInfoVo.prototype.onLineTime = undefined;

/**
 * 设备的在线状态,true:在线，false 离线
 * @member {Boolean} onlineStatus
 */
DeviceStatusInfoVo.prototype.onlineStatus = undefined;

/**
 * plc的状态信息
 * @member {Array.<module:com.mn.sdk.model/PlcStatusVo>} plcList
 */
DeviceStatusInfoVo.prototype.plcList = undefined;


