/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The UpdateUserForm model module.
 * @module com.mn.sdk.model/UpdateUserForm
 * @version 1.0
 */
export class UpdateUserForm {
  /**
   * Constructs a new <code>UpdateUserForm</code>.
   * 修改用户信息Form
   * @alias module:com.mn.sdk.model/UpdateUserForm
   * @class
   * @param address {String} 地址
   * @param departmentId {String} 部门ID
   * @param email {String} 电子邮箱
   * @param id {String} 用户id
   * @param lock {Boolean} 账号是否锁定(1:锁定 0未锁定)
   * @param nickname {String} 昵称
   * @param officePhone {String} 办公电话
   * @param oldPhone {String} 旧手机号
   * @param phone {String} 手机号
   * @param phoneCode {String} 手机验证码
   * @param roleId {String} 角色ID
   * @param sex {Number} 性别(0:男 1:女 2:未知)
   * @param source {Number} 数据来源(0注册审核,1企业管理菜单中手动添加,2超级用户添加企业时生成,3系统自动添加)
   * @param type {Number} 类型(1管理员 2普通用户)与实际的type不一样
   * @param username {String} 用户名
   */
  constructor(address, departmentId, email, id, lock, nickname, officePhone, oldPhone, phone, phoneCode, roleId, sex, source, type, username) {
    this.address = address;
    this.departmentId = departmentId;
    this.email = email;
    this.id = id;
    this.lock = lock;
    this.nickname = nickname;
    this.officePhone = officePhone;
    this.oldPhone = oldPhone;
    this.phone = phone;
    this.phoneCode = phoneCode;
    this.roleId = roleId;
    this.sex = sex;
    this.source = source;
    this.type = type;
    this.username = username;
  }


    static constructFromList(list) {
        let array = [new UpdateUserForm()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(UpdateUserForm.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>UpdateUserForm</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/UpdateUserForm} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/UpdateUserForm} The populated <code>UpdateUserForm</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new UpdateUserForm();
      if (Reflect.has(data, 'address'))
        obj.address = ApiClient.convertToType(data['address'], 'String');
      if (Reflect.has(data, 'departmentId'))
        obj.departmentId = ApiClient.convertToType(data['departmentId'], 'String');
      if (Reflect.has(data, 'email'))
        obj.email = ApiClient.convertToType(data['email'], 'String');
      if (Reflect.has(data, 'id'))
        obj.id = ApiClient.convertToType(data['id'], 'String');
      if (Reflect.has(data, 'lock'))
        obj.lock = ApiClient.convertToType(data['lock'], 'Boolean');
      if (Reflect.has(data, 'nickname'))
        obj.nickname = ApiClient.convertToType(data['nickname'], 'String');
      if (Reflect.has(data, 'officePhone'))
        obj.officePhone = ApiClient.convertToType(data['officePhone'], 'String');
      if (Reflect.has(data, 'oldPhone'))
        obj.oldPhone = ApiClient.convertToType(data['oldPhone'], 'String');
      if (Reflect.has(data, 'phone'))
        obj.phone = ApiClient.convertToType(data['phone'], 'String');
      if (Reflect.has(data, 'phoneCode'))
        obj.phoneCode = ApiClient.convertToType(data['phoneCode'], 'String');
      if (Reflect.has(data, 'roleId'))
        obj.roleId = ApiClient.convertToType(data['roleId'], 'String');
      if (Reflect.has(data, 'sex'))
        obj.sex = ApiClient.convertToType(data['sex'], 'Number');
      if (Reflect.has(data, 'source'))
        obj.source = ApiClient.convertToType(data['source'], 'Number');
      if (Reflect.has(data, 'type'))
        obj.type = ApiClient.convertToType(data['type'], 'Number');
      if (Reflect.has(data, 'username'))
        obj.username = ApiClient.convertToType(data['username'], 'String');
    }
    return obj;
  }
}

/**
 * 地址
 * @member {String} address
 */
UpdateUserForm.prototype.address = undefined;

/**
 * 部门ID
 * @member {String} departmentId
 */
UpdateUserForm.prototype.departmentId = undefined;

/**
 * 电子邮箱
 * @member {String} email
 */
UpdateUserForm.prototype.email = undefined;

/**
 * 用户id
 * @member {String} id
 */
UpdateUserForm.prototype.id = undefined;

/**
 * 账号是否锁定(1:锁定 0未锁定)
 * @member {Boolean} lock
 */
UpdateUserForm.prototype.lock = undefined;

/**
 * 昵称
 * @member {String} nickname
 */
UpdateUserForm.prototype.nickname = undefined;

/**
 * 办公电话
 * @member {String} officePhone
 */
UpdateUserForm.prototype.officePhone = undefined;

/**
 * 旧手机号
 * @member {String} oldPhone
 */
UpdateUserForm.prototype.oldPhone = undefined;

/**
 * 手机号
 * @member {String} phone
 */
UpdateUserForm.prototype.phone = undefined;

/**
 * 手机验证码
 * @member {String} phoneCode
 */
UpdateUserForm.prototype.phoneCode = undefined;

/**
 * 角色ID
 * @member {String} roleId
 */
UpdateUserForm.prototype.roleId = undefined;

/**
 * 性别(0:男 1:女 2:未知)
 * @member {Number} sex
 */
UpdateUserForm.prototype.sex = undefined;

/**
 * 数据来源(0注册审核,1企业管理菜单中手动添加,2超级用户添加企业时生成,3系统自动添加)
 * @member {Number} source
 */
UpdateUserForm.prototype.source = undefined;

/**
 * 类型(1管理员 2普通用户)与实际的type不一样
 * @member {Number} type
 */
UpdateUserForm.prototype.type = undefined;

/**
 * 用户名
 * @member {String} username
 */
UpdateUserForm.prototype.username = undefined;


