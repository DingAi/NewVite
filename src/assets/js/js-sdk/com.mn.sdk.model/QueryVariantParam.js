/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The QueryVariantParam model module.
 * @module com.mn.sdk.model/QueryVariantParam
 * @version 1.0
 */
export class QueryVariantParam {
  /**
   * Constructs a new <code>QueryVariantParam</code>.
   * 查询变量信息
   * @alias module:com.mn.sdk.model/QueryVariantParam
   * @class
   * @param currentPage {Number} 当前页
   * @param keyword {String} 模糊搜索变量id/名称
   * @param pageSize {Number} 分页大小
   * @param resourceIds {Array.<String>} 资源id列表,由resourceType指定类型
   * @param resourceType {String} 资源类型，可选【project,template,device,variantFavorite,condition】
   * @param saveType {String} 类型 可传值：[0,1,2,3]对应【0.不存储，1.周期存储（需要制定saveValue周期时间）,2.变化存储，3.条件存储】
   * @param saveValue {String} 值，在saveType指定为1时需要指定savaValue
   * @param variantGroupIds {Array.<String>} 变量分组id
   * @param variantLevel {String} 控制等级
   * @param writeable {String} 可读/读写
   */
  constructor(currentPage, keyword, pageSize, resourceIds, resourceType, saveType, saveValue, variantGroupIds, variantLevel, writeable) {
    this.currentPage = currentPage;
    this.keyword = keyword;
    this.pageSize = pageSize;
    this.resourceIds = resourceIds;
    this.resourceType = resourceType;
    this.saveType = saveType;
    this.saveValue = saveValue;
    this.variantGroupIds = variantGroupIds;
    this.variantLevel = variantLevel;
    this.writeable = writeable;
  }


    static constructFromList(list) {
        let array = [new QueryVariantParam()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(QueryVariantParam.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>QueryVariantParam</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/QueryVariantParam} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/QueryVariantParam} The populated <code>QueryVariantParam</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new QueryVariantParam();
      if (Reflect.has(data, 'currentPage'))
        obj.currentPage = ApiClient.convertToType(data['currentPage'], 'Number');
      if (Reflect.has(data, 'keyword'))
        obj.keyword = ApiClient.convertToType(data['keyword'], 'String');
      if (Reflect.has(data, 'pageSize'))
        obj.pageSize = ApiClient.convertToType(data['pageSize'], 'Number');
      if (Reflect.has(data, 'resourceIds'))
        obj.resourceIds = ApiClient.convertToType(data['resourceIds'], ['String']);
      if (Reflect.has(data, 'resourceType'))
        obj.resourceType = ApiClient.convertToType(data['resourceType'], 'String');
      if (Reflect.has(data, 'saveType'))
        obj.saveType = ApiClient.convertToType(data['saveType'], 'String');
      if (Reflect.has(data, 'saveValue'))
        obj.saveValue = ApiClient.convertToType(data['saveValue'], 'String');
      if (Reflect.has(data, 'variantGroupIds'))
        obj.variantGroupIds = ApiClient.convertToType(data['variantGroupIds'], ['String']);
      if (Reflect.has(data, 'variantLevel'))
        obj.variantLevel = ApiClient.convertToType(data['variantLevel'], 'String');
      if (Reflect.has(data, 'writeable'))
        obj.writeable = ApiClient.convertToType(data['writeable'], 'String');
    }
    return obj;
  }
}

/**
 * 当前页
 * @member {Number} currentPage
 */
QueryVariantParam.prototype.currentPage = undefined;

/**
 * 模糊搜索变量id/名称
 * @member {String} keyword
 */
QueryVariantParam.prototype.keyword = undefined;

/**
 * 分页大小
 * @member {Number} pageSize
 */
QueryVariantParam.prototype.pageSize = undefined;

/**
 * 资源id列表,由resourceType指定类型
 * @member {Array.<String>} resourceIds
 */
QueryVariantParam.prototype.resourceIds = undefined;

/**
 * 资源类型，可选【project,template,device,variantFavorite,condition】
 * @member {String} resourceType
 */
QueryVariantParam.prototype.resourceType = undefined;

/**
 * 类型 可传值：[0,1,2,3]对应【0.不存储，1.周期存储（需要制定saveValue周期时间）,2.变化存储，3.条件存储】
 * @member {String} saveType
 */
QueryVariantParam.prototype.saveType = undefined;

/**
 * 值，在saveType指定为1时需要指定savaValue
 * @member {String} saveValue
 */
QueryVariantParam.prototype.saveValue = undefined;

/**
 * 变量分组id
 * @member {Array.<String>} variantGroupIds
 */
QueryVariantParam.prototype.variantGroupIds = undefined;

/**
 * 控制等级
 * @member {String} variantLevel
 */
QueryVariantParam.prototype.variantLevel = undefined;

/**
 * 可读/读写
 * @member {String} writeable
 */
QueryVariantParam.prototype.writeable = undefined;


