/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The SaveVariantFavoriteForm model module.
 * @module com.mn.sdk.model/SaveVariantFavoriteForm
 * @version 1.0
 */
export class SaveVariantFavoriteForm {
  /**
   * Constructs a new <code>SaveVariantFavoriteForm</code>.
   * 收藏分组
   * @alias module:com.mn.sdk.model/SaveVariantFavoriteForm
   * @class
   * @param favoriteId {String} 收藏组id（修改时需要）
   * @param resourceId {String} 资源id 可传projectId或templateId
   * @param resourceType {String} 资源类型 project或template
   * @param variantFavoriteName {String} 收藏分组名称
   * @param variantIds {Array.<String>} 变量Id与设备Id拼接, 拼接内容 = 'deviceId:variantId' 
   */
  constructor(favoriteId, resourceId, resourceType, variantFavoriteName, variantIds) {
    this.favoriteId = favoriteId;
    this.resourceId = resourceId;
    this.resourceType = resourceType;
    this.variantFavoriteName = variantFavoriteName;
    this.variantIds = variantIds;
  }


    static constructFromList(list) {
        let array = [new SaveVariantFavoriteForm()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(SaveVariantFavoriteForm.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>SaveVariantFavoriteForm</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/SaveVariantFavoriteForm} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/SaveVariantFavoriteForm} The populated <code>SaveVariantFavoriteForm</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new SaveVariantFavoriteForm();
      if (Reflect.has(data, 'favoriteId'))
        obj.favoriteId = ApiClient.convertToType(data['favoriteId'], 'String');
      if (Reflect.has(data, 'resourceId'))
        obj.resourceId = ApiClient.convertToType(data['resourceId'], 'String');
      if (Reflect.has(data, 'resourceType'))
        obj.resourceType = ApiClient.convertToType(data['resourceType'], 'String');
      if (Reflect.has(data, 'variantFavoriteName'))
        obj.variantFavoriteName = ApiClient.convertToType(data['variantFavoriteName'], 'String');
      if (Reflect.has(data, 'variantIds'))
        obj.variantIds = ApiClient.convertToType(data['variantIds'], ['String']);
    }
    return obj;
  }
}

/**
 * 收藏组id（修改时需要）
 * @member {String} favoriteId
 */
SaveVariantFavoriteForm.prototype.favoriteId = undefined;

/**
 * 资源id 可传projectId或templateId
 * @member {String} resourceId
 */
SaveVariantFavoriteForm.prototype.resourceId = undefined;

/**
 * 资源类型 project或template
 * @member {String} resourceType
 */
SaveVariantFavoriteForm.prototype.resourceType = undefined;

/**
 * 收藏分组名称
 * @member {String} variantFavoriteName
 */
SaveVariantFavoriteForm.prototype.variantFavoriteName = undefined;

/**
 * 变量Id与设备Id拼接, 拼接内容 = 'deviceId:variantId' 
 * @member {Array.<String>} variantIds
 */
SaveVariantFavoriteForm.prototype.variantIds = undefined;


