/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The SuperUserVo model module.
 * @module com.mn.sdk.model/SuperUserVo
 * @version 1.0
 */
export class SuperUserVo {
  /**
   * Constructs a new <code>SuperUserVo</code>.
   * super后台用户Vo
   * @alias module:com.mn.sdk.model/SuperUserVo
   * @class
   * @param companyName {String} 企业名称
   * @param createdTime {Date} 创建时间
   * @param email {String} 邮箱
   * @param lock {String} 状态(账号是否被锁定 1:被锁定、0是未锁定)
   * @param loginTime {Date} 上次上线时间
   * @param nickname {String} 昵称
   * @param phone {String} 手机号
   * @param userId {String} 用户id
   * @param username {String} 用户名
   */
  constructor(companyName, createdTime, email, lock, loginTime, nickname, phone, userId, username) {
    this.companyName = companyName;
    this.createdTime = createdTime;
    this.email = email;
    this.lock = lock;
    this.loginTime = loginTime;
    this.nickname = nickname;
    this.phone = phone;
    this.userId = userId;
    this.username = username;
  }


    static constructFromList(list) {
        let array = [new SuperUserVo()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(SuperUserVo.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>SuperUserVo</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/SuperUserVo} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/SuperUserVo} The populated <code>SuperUserVo</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new SuperUserVo();
      if (Reflect.has(data, 'companyName'))
        obj.companyName = ApiClient.convertToType(data['companyName'], 'String');
      if (Reflect.has(data, 'createdTime'))
        obj.createdTime = ApiClient.convertToType(data['createdTime'], 'Date');
      if (Reflect.has(data, 'email'))
        obj.email = ApiClient.convertToType(data['email'], 'String');
      if (Reflect.has(data, 'lock'))
        obj.lock = ApiClient.convertToType(data['lock'], 'String');
      if (Reflect.has(data, 'loginTime'))
        obj.loginTime = ApiClient.convertToType(data['loginTime'], 'Date');
      if (Reflect.has(data, 'nickname'))
        obj.nickname = ApiClient.convertToType(data['nickname'], 'String');
      if (Reflect.has(data, 'phone'))
        obj.phone = ApiClient.convertToType(data['phone'], 'String');
      if (Reflect.has(data, 'userId'))
        obj.userId = ApiClient.convertToType(data['userId'], 'String');
      if (Reflect.has(data, 'username'))
        obj.username = ApiClient.convertToType(data['username'], 'String');
    }
    return obj;
  }
}

/**
 * 企业名称
 * @member {String} companyName
 */
SuperUserVo.prototype.companyName = undefined;

/**
 * 创建时间
 * @member {Date} createdTime
 */
SuperUserVo.prototype.createdTime = undefined;

/**
 * 邮箱
 * @member {String} email
 */
SuperUserVo.prototype.email = undefined;

/**
 * 状态(账号是否被锁定 1:被锁定、0是未锁定)
 * @member {String} lock
 */
SuperUserVo.prototype.lock = undefined;

/**
 * 上次上线时间
 * @member {Date} loginTime
 */
SuperUserVo.prototype.loginTime = undefined;

/**
 * 昵称
 * @member {String} nickname
 */
SuperUserVo.prototype.nickname = undefined;

/**
 * 手机号
 * @member {String} phone
 */
SuperUserVo.prototype.phone = undefined;

/**
 * 用户id
 * @member {String} userId
 */
SuperUserVo.prototype.userId = undefined;

/**
 * 用户名
 * @member {String} username
 */
SuperUserVo.prototype.username = undefined;


