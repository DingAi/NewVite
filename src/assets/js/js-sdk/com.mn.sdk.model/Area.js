/*
 * 系统接口
 * 系统接口文档
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 4.0.0
 *
 * Do not edit the class manually.
 *
 */

import {ApiClient} from '../ApiClient';

/**
 * The Area model module.
 * @module com.mn.sdk.model/Area
 * @version 1.0
 */
export class Area {
  /**
   * Constructs a new <code>Area</code>.
   * 区域代码表
   * @alias module:com.mn.sdk.model/Area
   * @class
   * @param alias {String} 别名
   * @param code {String} 区域代码
   * @param latitude {String} 纬度
   * @param level {String} 等级
   * @param longitude {String} 经度
   * @param name {String} 区域名称
   * @param parentCode {String} 父级区域代码
   */
  constructor(alias, code, latitude, level, longitude, name, parentCode) {
    this.alias = alias;
    this.code = code;
    this.latitude = latitude;
    this.level = level;
    this.longitude = longitude;
    this.name = name;
    this.parentCode = parentCode;
  }


    static constructFromList(list) {
        let array = [new Area()];
        array.splice(0, array.length);

        if (!(list instanceof Array)) {
            return array;
        }

        for (const obj of list) {
            array.push(Area.constructFromObject(obj));
        }

        return array;
    }


  /**
   * Constructs a <code>Area</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:com.mn.sdk.model/Area} obj Optional instance to populate.
   * @return {module:com.mn.sdk.model/Area} The populated <code>Area</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new Area();
      if (Reflect.has(data, 'alias'))
        obj.alias = ApiClient.convertToType(data['alias'], 'String');
      if (Reflect.has(data, 'code'))
        obj.code = ApiClient.convertToType(data['code'], 'String');
      if (Reflect.has(data, 'latitude'))
        obj.latitude = ApiClient.convertToType(data['latitude'], 'String');
      if (Reflect.has(data, 'level'))
        obj.level = ApiClient.convertToType(data['level'], 'String');
      if (Reflect.has(data, 'longitude'))
        obj.longitude = ApiClient.convertToType(data['longitude'], 'String');
      if (Reflect.has(data, 'name'))
        obj.name = ApiClient.convertToType(data['name'], 'String');
      if (Reflect.has(data, 'parent_code'))
        obj.parentCode = ApiClient.convertToType(data['parent_code'], 'String');
    }
    return obj;
  }
}

/**
 * 别名
 * @member {String} alias
 */
Area.prototype.alias = undefined;

/**
 * 区域代码
 * @member {String} code
 */
Area.prototype.code = undefined;

/**
 * 纬度
 * @member {String} latitude
 */
Area.prototype.latitude = undefined;

/**
 * 等级
 * @member {String} level
 */
Area.prototype.level = undefined;

/**
 * 经度
 * @member {String} longitude
 */
Area.prototype.longitude = undefined;

/**
 * 区域名称
 * @member {String} name
 */
Area.prototype.name = undefined;

/**
 * 父级区域代码
 * @member {String} parentCode
 */
Area.prototype.parentCode = undefined;


