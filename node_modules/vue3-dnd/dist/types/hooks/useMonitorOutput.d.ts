import type { HandlerManager, MonitorEventEmitter } from '../types';
import { Ref } from 'vue-demi';
import { MaybeRef } from '../types/utils';
export declare function useMonitorOutput<Monitor extends HandlerManager, Collected>(monitor: MaybeRef<Monitor & MonitorEventEmitter>, collect: MaybeRef<(monitor: Monitor) => Collected>, onCollect?: () => void): Ref<Collected>;
