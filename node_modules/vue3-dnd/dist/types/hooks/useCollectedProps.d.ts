import type { Connector } from '../internals';
import type { HandlerManager, MonitorEventEmitter } from '../types';
import { MaybeRef } from '../types/utils';
export declare function useCollectedProps<Collected, Monitor extends HandlerManager>(collector: MaybeRef<(monitor: Monitor) => Collected> | undefined, monitor: MaybeRef<Monitor & MonitorEventEmitter>, connector: MaybeRef<Connector>): import("vue-demi").Ref<Collected>;
